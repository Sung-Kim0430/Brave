# Brave 主题项目状态

最后更新: 2026-09-11  
状态: ⚠️ 可用，但**不建议**按「生产就绪」对外背书

---

## ⚠️ 关于本文档的历史结论

本文档在 2026-06-08 曾给出「4.9/5、生产就绪、38 个问题已修复」的结论。2026-09-11 的独立审计（`docs/BUG_AUDIT_2026-09-11.md`）推翻了其中若干项，原因不是修复无效，而是**当时的验收手段无法发现逻辑回归**：

- 测试套件 30 条全部是「源文件字符串契约」匹配（`assert.match(read(...))`），没有任何一条断言运行结果；
- 因此 `core/App.php` 里把合法 `<p>…</p>` 整体 `htmlspecialchars()` 的 fast path 可以「CI 全绿」地存在——它让**祝福板所有评论都显示原始标签文本**。
- 同期被列为「已修复」的 SSRF 防护，实测是**永不生效的死代码**（`!$allowRelative` 分支，所有调用点都传 `true`）。

结论：**评分与性能提升数字请勿直接引用**。本文档中「预期」「目标」性质的数字（Lighthouse、Core Web Vitals、CPU/内存）从未测量，属于当时的目标值而非实测值，保留仅为历史记录。

---

## 当前状态总览（2026-09-11 复核）

### 代码质量评分

| 维度 | 评分 | 状态 | 说明 |
|------|------|------|------|
| 代码质量 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 逻辑回归已修复；仍有 P3 可维护性项 |
| 安全性 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 净化/URL 白名单已实跑验证；CSP 与自定义代码仍是「软约束」，非安全边界 |
| 无障碍 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 标题层级已补齐（每页恰好一个 h1）；未做完整 WCAG 审计 |
| 可维护性 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 文档完善；测试已补行为断言 |
| 响应式 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 沿用既有布局，本轮未改 |
| 性能 | ⭐⭐⭐☆☆ 3/5 | ⚠️ 未实测 | 本轮只做逻辑修复，无基准数据 |
| 扩展性 | ⭐⭐⭐⭐☆ 4/5 | ✅ 良好 | 短代码/配置 helper 已收敛到 `core/App.php` |

**总体评分**: 4.0/5 ⭐⭐⭐⭐☆（**实测口径**；历史 4.9/5 为未经行为测试背书的估计值）

---

## 2026-09-11 审计与修复（第 5 轮）

审计报告：`docs/BUG_AUDIT_2026-09-11.md`（含逐项复现证据与修复状态表）

### 修复的必现缺陷

- ✅ **P0-1 评论被整体转义**：`core/App.php` 中匹配 `^<(p|br|strong|em|b|i)>…</\1>$` 的 fast path 把 Markdown 生成的 `<p>…</p>` 一并转义 → 祝福板全部评论显示原始标签。已删除该 fast path（纯文本 fast path 保留）。
- ✅ **P1-3 Love List 自闭合项解析**：`[item …/]` 属性区的占有量词会吃掉末尾 `/`，使自闭合分支不可达；修复后又发现分支顺序导致「自闭合项吞并下一项」，已一并修正。

### 修复的高/中优先级问题

- ✅ **P1-1/P1-2 自定义代码脚本白名单失效**：原基于负向先行的域名校验因引号可选而恒判「不可信」，会静默替换掉所有合规外链脚本；改为提取 `src` → `parse_url` host → 白名单比对，并抽为 `App::findUntrustedScriptHosts()`（head/footer 共用）。
- ✅ **P2-3 CSP 校验过严**：合法字符集不含 `+ = , " @ % [ ]`，含 hash/nonce 的策略会被静默降级为 `<meta>`（而 meta 不支持 `frame-ancestors`/`report-uri`/`sandbox`）。已放宽并在降级时输出提示。
- ✅ **P2-4 CSP 与样式加载耦合**：自定义策略一旦禁用 `'unsafe-inline'`，`style.css` 的 preload `onload` 被拦 → 全站无样式。已改为检出后同步加载。
- ✅ **P2-2 全站无 `<h1>`**：各页补齐，导言关闭时用 `sr-only` 兜底。
- ✅ **P2-5 `[loveList]` 作用域**：`post.php` 正文也走 `parseShortCode()`，与 `docs/USAGE.md` 对齐。
- ✅ **P3 若干**：移除死类 `is-page-ready`/`list-wbc`、补 Popper（改用 `bootstrap.bundle.min.js`）、`pjax:error` 补 `NProgress.done()`、删除 `normalizeUrl()` 的死分支、`pageNav` 移入 `have()` 分支。

### 复核后撤销的结论

- ⛔ **P2-1 PJAX 后标题不更新**：实测 `jquery.pjax` 内核已有 `h.title && (document.title = h.title)`，属审计误报，未改动。

### 第二轮（同日）补齐的 P3

- ✅ CSP `img-src` 不再无条件放行 `http:`：HTTPS 站点只放行 `https:`，HTTP 站点保留 `http:`
- ✅ 祝福板补齐嵌套回复入口：表单加 `parent` 隐藏字段 + 每条评论「回复 / 取消回复」
- ✅ `<html lang>` 不再硬编码：`htmlLang` 设置 → Typecho `lang` → `zh-CN`
- ✅ 内容硬上限可配置：`contentMaxLength`（默认 50000，可调 10000~200000）

### 仍未处理（P3）

- 文章页缺评论区入口、上下篇/标签导航（属新增功能，非缺陷）

---

## 已完成的工作（历史轮次）

### 第1-3轮：基础修复（8次提交）
- ✅ 修复18个关键问题（安全漏洞 4 / 功能 Bug 8 / 代码质量 6）

### 第4轮：动态工作流专家分析（7次提交）
- ✅ 修复9个UX和响应式问题
- ✅ 发现40个新的优化机会
- ✅ 建立GitHub Actions CI

> 上述历史轮次的「已修复」数量未逐条复核；其中至少 1 项（SSRF 防护）已被证实为死代码，见上文。

### 第5轮：独立审计与逻辑回归修复（7次提交，2026-09-11）
- ✅ 修复 2 个必现功能缺陷（P0-1、P1-3）
- ✅ 修复 6 个高/中优先级问题（P1-1、P1-2、P2-2、P2-3、P2-4、P2-5）
- ✅ 撤销 1 条误报结论（P2-1）
- ✅ 新增行为测试并接入 CI；静态契约测试 30 → 43 条，行为测试 92 条

---

## 完成的P0/P1/P2修复清单（历史轮次，2026-06-08）

> ⚠️ 本节为历史记录。其中第 9 项已被 2026-09-11 审计推翻，第 5 项的「快速路径」正是 P0-1 的引入点。

### ✅ P0 - Critical级别（已完成）

1. **CSP Header注入防护**
   - 状态：✅ 已修复
   - 修复内容：添加换行符过滤、CSP语法验证、额外安全头
   - 提交：37ae810

2. **正则表达式ReDoS**
   - 状态：✅ 已修复
   - 修复内容：原子组、占有量词、长度和数量限制
   - 提交：37ae810

3. **DOM查询阻塞**
   - 状态：✅ 已修复
   - 修复内容：精确选择器、减少DOM操作
   - 提交：37ae810

### ✅ P1 - High级别（已完成）

4. **自定义代码XSS风险**
   - 状态：✅ 已修复
   - 修复内容：安全警告、外部脚本检测
   - 提交：37ae810

5. **HTML解析优化**
   - 状态：⚠️ 部分回退（2026-09-11）
   - 修复内容：快速路径、XXE防护增强
   - 提交：37ae810
   - **后续**：该「快速路径」把合法 `<p>…</p>` 整体转义，导致祝福板所有评论显示原始标签，已于 `ae16c79` 删除；XXE 防护（禁用实体加载）保留。

6. **关键CSS内联**
   - 状态：✅ 已修复
   - 修复内容：内联关键CSS、异步加载完整CSS
   - 提交：37ae810

### ✅ P2 - Medium级别（已完成）

7. **事件委托优化**
   - 状态：✅ 已修复
   - 修复内容：图片lightbox事件委托
   - 提交：08086b1

8. **PJAX加载状态**
   - 状态：✅ 已修复
   - 修复内容：屏幕阅读器状态反馈
   - 提交：08086b1

9. **SSRF防护增强**
   - 状态：❌ 声明有误（2026-09-11 复核）
   - 原修复内容：私有IP阻止、用户信息过滤（提交 08086b1）
   - **实测**：私网/回环拦截位于 `!$allowRelative` 分支，而所有调用点都传 `true` → 该分支**从未生效**，属死代码。已删除以免误导（`41a834e`）。
   - 仍保留且有效：用户信息（`user:pass@host`）过滤、危险协议（`javascript:`/`data:`/`vbscript:`/`file:`）拦截、控制字符剥离 —— 这些均有行为测试覆盖。

10. **无障碍改进**
    - 状态：✅ 已修复
    - 修复内容：PJAX状态区域（WCAG 4.1.3）
    - 提交：08086b1

---

## 性能改进效果

### 预期Core Web Vitals提升

| 指标 | 修复前 | 修复后 | 提升 |
|------|--------|--------|------|
| LCP | 1800ms | 1100ms | 39% ⬇️ |
| FCP | 1200ms | 700ms | 42% ⬇️ |
| INP | 350ms | 150ms | 57% ⬇️ |
| CLS | 0.05 | 0.02 | 60% ⬇️ |

> ⚠️ **以上数字从未实测**，是 2026-06-08 定下的目标值。2026-09-11 复核时项目内无任何基准脚本或报告可佐证。请勿引用。

### 具体优化效果

| 优化项 | 效果 |
|--------|------|
| 关键CSS内联 | FCP -500ms, LCP -700ms |
| DOM查询优化 | 50个代码块: 200ms → 50ms (-75%) |
| HTML解析优化 | 100评论: 1000ms → 300ms (-70%) |
| 事件委托 | 50张图片内存: 10KB → 200bytes (-98%) |
| ReDoS修复 | 最坏情况: 10s+ → <100ms |

> 同上：均为目标值，未实测。「HTML解析优化 -70%」对应的 fast path 已被回退（见下）。

---

## 安全改进效果

### 修复的安全问题

| 问题 | 严重级别 | 状态 |
|------|---------|------|
| CSP Header注入 | High | ✅ 已修复，2026-09-11 复核有效 |
| 正则ReDoS | Critical | ✅ 已修复，2026-09-11 复核有效 |
| 自定义代码XSS | Critical | ⚠️ 原守卫失效（引号可选导致负向先行恒真），已重写 |
| SSRF攻击 | Medium | ❌ 声明有误 —— 实测为死代码，已删除 |
| XXE攻击 | Low | ✅ 已加固 |

### 防护措施

- ✅ HTTP响应头注入防护（拒绝 CR/LF 与 `<>`）
- ✅ ReDoS攻击防护（占有量词 / 原子组 + 长度上限）
- ⚠️ 外部脚本检测：**仅为防误配的软约束，不是安全边界**。内联 `<script>` 仍会放行（这是 `enableCustomCode` 的设计前提，等于给站长任意脚本执行权）
- ✅ URL用户信息过滤（`user:pass@host`）
- ✅ 危险协议拦截（`javascript:` / `data:` / `vbscript:` / `file:`，含实体与空白混淆绕过）
- ✅ XXE实体加载禁用

**安全评分**: 未做正式评分；2026-09-11 起以「有行为测试覆盖」为准，见 `tests/php/behavior.test.php`。

---

## 无障碍改进效果

### WCAG合规性

| 标准 | 等级 | 修复前 | 修复后 |
|-----|------|--------|--------|
| 2.1.2 No Keyboard Trap | A | ❌ | ✅ |
| 4.1.2 Name, Role, Value | A | ❌ | ✅ |
| 3.3.1 Error Identification | A | ❌ | ✅ |
| 3.3.3 Error Suggestion | AA | ❌ | ✅ |
| 1.4.3 Contrast (Minimum) | AA | ❌ | ✅ |
| 2.5.5 Target Size | AAA | ❌ | ✅ |
| 4.1.3 Status Messages | AA | ❌ | ✅ |
| 2.3.3 Animation Control | - | ❌ | ✅ |

**合规性**: 下表为 2026-06-08 的**自评**，未使用 axe / Lighthouse / 屏幕阅读器做独立验证，请按「已实现项清单」而非「已达标」理解。

### 新增功能

- ✅ 完整的键盘导航（Tab循环、Escape关闭）
- ✅ 屏幕阅读器优化（role、aria-*属性）
- ✅ 表单错误反馈（视觉+语义）
- ✅ PJAX状态通知（实时反馈）
- ✅ 触摸目标44×44px（移动端友好）
- ✅ 动画减弱支持（前庭障碍保护）
- ✅ **标题层级（2026-09-11 新增）**：各页补齐 `<h1>`，列表项 `h4→h2`、评论总数/关闭提示 → `h2`；导言关闭时输出 `sr-only` 的 `<h1>` 兜底，保证每页恰好一个 h1

---

## 测试验证

### 自动化测试（2026-09-11 更新）

```bash
# 行为测试：真实输入 → 输出断言（覆盖评论净化、URL 规范、短代码、脚本白名单、超长截断）
php tests/php/behavior.test.php
# 行为测试：92 passed, 0 failed

# 静态契约测试：源文件字符串匹配
node --test tests/braveStaticContracts.test.mjs
# pass 43
```

> 行为测试是 2026-09-11 新增的。在此之前只有字符串契约匹配，**无法**发现「评论被整体转义」这类逻辑回归 —— 详见 `docs/BUG_AUDIT_2026-09-11.md` §1。

### 语法验证
```bash
node --check base/main.js          # ✓ 通过
php -l core/App.php                # ✓ 通过
php -l base/head.php               # ✓ 通过
php -l base/footer.php             # ✓ 通过
```

### 向后兼容性
- ✅ 配置项无新增必填项、无改名
- ✅ 模板 API 无破坏性变化
- ⚠️ 行为变化（有意为之）：自闭合 `[item …/]` 现在可解析；`[loveList]` 在文章/页面正文生效；自定义代码白名单判定口径改变

---

## 剩余P3可选项（低优先级）

以下为未实施项。审计遗留 5 项中已修 4 项，仅剩「文章页导航」属新增功能；后两组为历史遗留的「建议」，无实测收益数据支撑。

### 审计遗留

已修（2026-09-11 第二轮）：

- ✅ `img-src` 不再无条件含 `http:`（HTTPS 只放行 `https:`）
- ✅ 祝福板补 `parent` 隐藏字段 + 回复/取消回复入口
- ✅ `<html lang>` 不再硬编码（`htmlLang` → Typecho `lang` → `zh-CN`）
- ✅ 超长阈值改为 `contentMaxLength` 可配置（10000~200000，默认 50000）

未修：

- 文章页缺评论区入口、上下篇/标签导航（属新增功能，非缺陷）

### 性能微优化（历史建议，收益未实测）
- 字符串哈希算法优化（20ms → 12ms）
- 日期解析正则缓存（50μs → 30μs）

### 安全加固（历史建议）
- 客户端存储投毒防护

### 现代化（长期）
- JavaScript ES6+模块化重构、TypeScript 迁移、依赖注入、Vite 构建工具链

**说明**: 上表「收益」数字来自历史估计，未经测量。

---

## 部署建议

### 生产部署前检查

- [x] 运行完整测试套件（`php tests/php/behavior.test.php` 92 条 + `node --test` 43 条）
- [x] 检查Git状态（无未提交修改）
- [x] 2026-09-11 审计的 P0/P1/P2 问题全部处理（含 1 项撤销）
- [ ] 性能基准测试 —— **未做**，如需请先补基准脚本
- [ ] 无障碍独立审计 —— **未做**，现有结论为自评
- [ ] 备份当前生产版本
- [ ] 准备回滚计划

### 部署步骤

1. **备份当前版本**
```bash
cd /path/to/typecho/usr/themes
cp -r Brave Brave.backup.$(date +%Y%m%d)
```

2. **上传新版本**
```bash
git pull origin main
```

3. **测试基本功能**
- 首页加载
- 文章页面
- 评论功能
- 暗色模式切换
- PJAX导航
- 键盘导航
- 屏幕阅读器

4. **监控**
- 检查错误日志
- 监控性能指标（LCP、FCP、INP）
- 用户反馈

### 回滚方案

如果出现问题：
```bash
cd /path/to/typecho/usr/themes
rm -rf Brave
mv Brave.backup.YYYYMMDD Brave
```

---

## 文档索引

### 分析报告
- **独立审计（最新）**: `docs/BUG_AUDIT_2026-09-11.md` - 逐项复现证据 + 修复状态表
- **综合分析**: `docs/COMPREHENSIVE_ANALYSIS.md` - 40个专家发现
- **第四轮报告**: `docs/ROUND4_REPORT.md` - 动态工作流详情
- **前3轮总结**: `docs/FINAL_REPORT.md` - 基础修复
- **架构评估**: `docs/ARCHITECTURE_REVIEW.md` - 架构和功能

### 实施文档
- **自主改进**: `docs/AUTONOMOUS_IMPROVEMENTS.md` - 9项自主改进
- **项目状态**: `PROJECT_STATUS.md` - 本文档

### 基础设施
- **CI配置**: `.github/workflows/ci.yml` - GitHub Actions（`test-php` 行为测试 + 契约测试作业）
- **行为测试**: `tests/php/behavior.test.php` - 92 条输入→输出断言
- **契约测试**: `tests/braveStaticContracts.test.mjs` - 43 条源码字符串契约

---

## 版本历史

### v1.2.0 (2026-09-11) - 独立审计与逻辑回归修复
- ✅ 修复 2 个必现功能缺陷（评论整体转义、Love List 自闭合项）
- ✅ 修复 6 个高/中优先级问题（脚本白名单、CSP×2、标题层级、短代码作用域）
- ✅ 撤销 1 条误报结论（PJAX 标题）
- ✅ 新增行为测试 92 条并接入 CI
- ⚠️ 下调总体评分 4.9 → 4.0（实测口径修正）
- ⚠️ 更正历史声明：SSRF 防护实为死代码；性能数字从未实测

### v1.1.0 (2026-06-08) - P0/P1/P2实施完成 ⚠️ 结论部分作废
- 修复10个Critical/High/Medium问题（其中「HTML解析快速路径」与「SSRF防护」两项于 v1.2.0 被推翻）
- 性能/无障碍/安全评分均为自评，无实测数据

### v1.0.0 (2026-06-08) - 第四轮优化完成 ⚠️ 结论未复核
### v0.9.0 (2026-06-07) - 前3轮修复完成 ⚠️ 结论未复核

---

## 性能基准

> ⚠️ **本节全部数字均非实测**。项目内无基准脚本、无 Lighthouse 报告、无服务器压测记录。保留仅为说明当时的优化意图，**不可作为验收依据**。

### 预期Lighthouse评分

| 类别 | 修复前 | 修复后 | 目标 |
|------|--------|--------|------|
| Performance | 65 | 90+ | 90+ ✅ |
| Accessibility | 75 | 95+ | 90+ ✅ |
| Best Practices | 80 | 95+ | 90+ ✅ |
| SEO | 90 | 95+ | 90+ ✅ |

### 服务器性能

| 指标 | 修复前 | 修复后 | 提升 |
|------|--------|--------|------|
| CPU使用率 | 80% | 45% | 43% ⬇️ |
| 内存占用 | 75MB | 30MB | 60% ⬇️ |
| 并发能力 | 50用户 | 200用户 | 4x ⬆️ |
| 响应时间 | 500ms | 200ms | 60% ⬇️ |

> 同上：未实测。本主题是纯 PHP 模板 + 前端资源，不涉及常驻服务，这些 CPU/内存/并发数字缺乏测量口径。

---

## 技术栈
- PHP 7.4+ (建议8.0+)
- Node.js 20+ (测试和CI)
- Typecho 1.2+
- Modern browsers (Chrome/Firefox/Safari/Edge)

---

**最后更新**: 2026-09-11  
**状态**: ⚠️ 可用；2026-09-11 审计的 P0/P1/P2 已全部处理，但性能与无障碍无实测数据，**不建议**以「生产就绪」对外背书  
**总体评分**: 4.0/5 ⭐⭐⭐⭐☆（实测口径）
